<view>
  <map id="map" longitude="{{ longitude }}" latitude="{{ latitude }}" scale="{{ scale }}" 
  markers="{{ markers }}"
  onMarkerTap="onMarkerTap"
  show-location style="width: 100%; height: 100vh;"></map>
</view>